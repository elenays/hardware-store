extends layout

    .basket
        each item in items
            .item-min__container
                a(class='item-min__href', href='goods/#{item._id}')

                .item-min__price #{item.price}